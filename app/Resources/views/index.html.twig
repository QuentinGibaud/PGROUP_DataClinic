{% extends 'base.html.twig' %}

{% block body %}
    <div class="wrapper">
        <div class="container">
            <div class="home-title">
              <h1 class="no-margin">{{ 'page.home.title'|trans|raw }}</h1>
            </div>
            <div>{{ 'page.home.description'|trans|raw }}</div>
        </div>
    </div>
{% endblock %}
