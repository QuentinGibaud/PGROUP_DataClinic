{% extends 'base.html.twig' %}

{% block body %}
    <div class="wrapper">
        <div class="container">
            <h1>{{ 'page.useTerms.title'|trans }}</h1>
            <div>{{ 'page.useTerms.description'|trans|raw }}</div>
        </div>
    </div>
{% endblock %}
